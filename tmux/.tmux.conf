set-option -g prefix C-a
bind-key C-t last-window
bind-key Space next-window
set -g base-index 1

# set -g escape-time 0

# set -g status-bg colour235
# set -g status-fg colour27
# set -g status-left "#[fg=colour128]> "
# set -g status-right-length 50
# set -g status-right "#[fg=colour8]B: #(dc -e \"2 l `cat /proc/acpi/battery/BAT0/state  | grep 'remaining capacity' | awk '{print $3}'` `cat /proc/acpi/battery/BAT0/state | grep 'present rate' | awk '{print $3'` / p\") #[fg=colour128]#S@#H [#[fg=colour8]%a %m%d %H:%M#[fg=colour128]] "

# set-window-option -g window-status-current-bg yellow
# set-window-option -g window-status-current-fg black

# set-window-option -g clock-mode-colour yellow

setw -g monitor-activity on
set -g visual-activity on

setw -g automatic-rename on

# set -g default-command /usr/local/bin/zsh
# set -g default-terminal "screen"

# Enable mouse controls
set -g mouse on
set -g set-titles on
set -g status on
# set -g mouse-select-window on
# set -g mouse-select-pane on
# set -g mouse-resize-pane on

# List of plugins 
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'arcticicestudio/nord-tmux'


# Initialize TMUX plugin manager (keep at bottom)
run -b '~/.tmux/plugins/tpm/tpm'
